{
  "name": "moneymap-api",
  "version": "1.0.0",
  "description": "Backend proxy for MoneyMap's market data providers (Finnhub for stocks/ETFs, CoinGecko for crypto). Keeps API keys server-side, caches responses, and rate-limits per client.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "node-fetch": "^2.7.0",
    "stripe": "^22.6.1"
  }
}
